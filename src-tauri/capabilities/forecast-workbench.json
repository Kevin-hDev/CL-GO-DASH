{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "forecast-workbench",
  "description": "Forecast workbench bound to the active Agent Local session",
  "windows": ["forecast-workbench"],
  "permissions": [
    "core:default"
  ]
}
